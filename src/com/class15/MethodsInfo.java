package com.class15;

public class MethodsInfo {
	
	/*//////////////////////////////////////////////////////////////////////////////////////////////////////
	 * =====================================Methods with RETURN value ======================================
	 * ****************************************************************************************************
	 * 
	 * Method with return values uses ==> return keyword 
	 * when we call method we have to pass matching argument of the method 
	 * and also we must follow the order of defined parameters
	 * 
	 * ===================////\\\\  RULES  \\\\////=====================
	 * 
	 * 1 . The return type is the must in a method, you can't declare the method without it's return type.
	 * 2.  The return statement should be the last statement in the method.
	 * 3.  You can't declare more than one return statement in one method.
	 * 4.  The data type of the returned value must match the type of the method's declared return value. 
	 * 5.  You can call the method with its name only.
	 * 6.  The method must have the body. 
	 * 7.  The method can accept the n number of parameters. 
	 * 8.  If the method has a number of parameter then it's the must to 
	       pass all parameter in method body while calling the method in program code. 
	  
	 * 9.  The variables declared inside the method body are called the local variable. 
	 * 10. Methods access modiﬁers deﬁne the access level of method.
	 * 11. If the return type is void it means method will not return any value.
	 
	  Syntax :
	  
	  returnType methodName(Parameters) {

	      code;
	      return value;
      }

      return value MUST be same type as returnType at the method header
	 */
	
	
	
	
	
	
	/*//////////////////////////////////////////////////////////////////////////////////////////////////////
	 * =====================================Methods with VOID value ======================================
	 * ****************************************************************************************************
	 * 
	 * Method without return any value uses==> void keyword
	 * we learnt that method in 14.class check the 14. package.
	 * when we call method we have to pass matching argument of the method 
	 * and also we must follow the order of defined parameters
	 
	 * syntax:
	 
	  void hello(){

 	  code;
      }
	 */

}
