package com.class27;

public class CollectionInfo {

	/*
	 * Collections are the containers that store and manipulate
	 * the group of objects that represented as a single unit.
	 * 
	 * This framework has several useful classes which have tons of useful functions
	 * which makes a programmer task super easy
	 * 
	 * Java Collection Framework is a collection of interfaces and classes which
	 * help in storing and processing the data efficiently.
	 * 
	 * The most frequently used for the test automation: List, Set, Map (map is not
	 * a collection but mostly used for collections)
	 * 
	 * ==> Why do we need collections? To manipulate data
	 * 
	 * ==> Collections:is an Interface it is granparent
	 * ==> Colection(Collection framework) consist of 3 Interfaces:
	 * ==> List, set and queue ==>interface and parent
	 * 
	 *========================= Commonly used methods of Collection interface:=======================
	 *public boolean add(object element), public boolean addAll(collection c), public boolean remove(object element)
	 *public boolean removeAll(Collection c),public boolean retainAll(Collection c)==> silmek istemediklerini tutmak
	 *digerleri silinecektir.
	 *public int size(), public void clear(), public boolean containsAll(collection c)
	 *public Iterator iterator():returns an iterator
	 */

}
